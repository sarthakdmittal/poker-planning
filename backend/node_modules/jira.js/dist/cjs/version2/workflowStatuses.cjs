'use strict';

class WorkflowStatuses {
    client;
    constructor(client) {
        this.client = client;
    }
    async getStatuses(callback) {
        const config = {
            url: '/rest/api/2/status',
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
    async getStatus(parameters, callback) {
        const idOrName = typeof parameters === 'string' ? parameters : parameters.idOrName;
        const config = {
            url: `/rest/api/2/status/${idOrName}`,
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.WorkflowStatuses = WorkflowStatuses;
//# sourceMappingURL=workflowStatuses.cjs.map
