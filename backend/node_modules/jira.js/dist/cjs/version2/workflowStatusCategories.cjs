'use strict';

class WorkflowStatusCategories {
    client;
    constructor(client) {
        this.client = client;
    }
    async getStatusCategories(callback) {
        const config = {
            url: '/rest/api/2/statuscategory',
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
    async getStatusCategory(parameters, callback) {
        const idOrKey = typeof parameters === 'string' ? parameters : parameters.idOrKey;
        const config = {
            url: `/rest/api/2/statuscategory/${idOrKey}`,
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.WorkflowStatusCategories = WorkflowStatusCategories;
//# sourceMappingURL=workflowStatusCategories.cjs.map
