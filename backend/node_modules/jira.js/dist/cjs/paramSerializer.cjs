'use strict';

function paramSerializer(key, values) {
    if (values === undefined || values === null)
        return undefined;
    if (Array.isArray(values)) {
        if (values.length === 0)
            return undefined;
        return values.map((v, i) => (i === 0 ? String(v) : `${key}=${String(v)}`)).join('&');
    }
    return encodeURIComponent(String(values));
}

exports.paramSerializer = paramSerializer;
//# sourceMappingURL=paramSerializer.cjs.map
