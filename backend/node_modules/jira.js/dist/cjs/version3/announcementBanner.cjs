'use strict';

class AnnouncementBanner {
    client;
    constructor(client) {
        this.client = client;
    }
    async getBanner(callback) {
        const config = {
            url: '/rest/api/3/announcementBanner',
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
    async setBanner(parameters, callback) {
        const config = {
            url: '/rest/api/3/announcementBanner',
            method: 'PUT',
            data: {
                isDismissible: parameters?.isDismissible,
                isEnabled: parameters?.isEnabled,
                message: parameters?.message,
                visibility: parameters?.visibility,
            },
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.AnnouncementBanner = AnnouncementBanner;
//# sourceMappingURL=announcementBanner.cjs.map
