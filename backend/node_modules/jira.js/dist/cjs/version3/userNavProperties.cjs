'use strict';

class UserNavProperties {
    client;
    constructor(client) {
        this.client = client;
    }
    async getUserNavProperty(parameters, callback) {
        const config = {
            url: `/rest/api/3/user/nav4-opt-property/${parameters.propertyKey}`,
            method: 'GET',
            params: {
                accountId: parameters.accountId,
            },
        };
        return this.client.sendRequest(config, callback);
    }
    async setUserNavProperty(parameters, callback) {
        const config = {
            url: `/rest/api/3/user/nav4-opt-property/${parameters.propertyKey}`,
            method: 'PUT',
            params: {
                accountId: parameters.accountId,
            },
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.UserNavProperties = UserNavProperties;
//# sourceMappingURL=userNavProperties.cjs.map
