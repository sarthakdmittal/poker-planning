'use strict';

var zod = require('zod');

/** Parameters for getting statuses by name. */
const GetStatusesByNameSchema = zod.z.object({
    /**
     * The list of status names.
     *
     * Min items `1`, Max items `50`
     */
    name: zod.z.array(zod.z.string()).min(1, 'At least one status name is required').max(50, 'Maximum 50 status names allowed'),
    /** The project the status is part of or null for global statuses. */
    projectId: zod.z.string().optional(),
});

exports.GetStatusesByNameSchema = GetStatusesByNameSchema;
//# sourceMappingURL=getStatusesByName.cjs.map
