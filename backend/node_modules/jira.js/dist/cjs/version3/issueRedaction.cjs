'use strict';

class IssueRedaction {
    client;
    constructor(client) {
        this.client = client;
    }
    async redact(parameters, callback) {
        const config = {
            url: '/rest/api/3/redact',
            method: 'POST',
            data: {
                redactions: parameters.redactions,
            },
        };
        return this.client.sendRequest(config, callback);
    }
    async getRedactionStatus(parameters, callback) {
        const config = {
            url: `/rest/api/3/redact/status/${parameters.jobId}`,
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.IssueRedaction = IssueRedaction;
//# sourceMappingURL=issueRedaction.cjs.map
