'use strict';

var zod = require('zod');
var projectId = require('./projectId.cjs');

/** The scope of the status. */
const StatusScopeSchema = zod.z.strictObject({
    /** The scope of the status. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    type: zod.z.enum(['GLOBAL', 'PROJECT']),
    project: projectId.ProjectIdSchema.optional(),
});

exports.StatusScopeSchema = StatusScopeSchema;
//# sourceMappingURL=statusScope.cjs.map
