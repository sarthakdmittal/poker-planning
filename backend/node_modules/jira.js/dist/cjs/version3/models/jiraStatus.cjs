'use strict';

var projectIssueTypes = require('./projectIssueTypes.cjs');
var statusScope = require('./statusScope.cjs');
var zod = require('zod');

/** Details of a status. */
const JiraStatusSchema = zod.z.strictObject({
    /** The ID of the status. */
    id: zod.z.string(),
    /** The name of the status. */
    name: zod.z.string(),
    /** The description of the status. */
    description: zod.z.string(),
    scope: statusScope.StatusScopeSchema,
    /** The category of the status. */
    statusCategory: zod.z.enum(['TODO', 'IN_PROGRESS', 'DONE']),
    /**
     * @deprecated See the [deprecation
     *   notice](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-2298) for details.
     *
     *   Projects and issue types where the status is used. Only available if the `usages` expand is requested.
     */
    usages: projectIssueTypes.ProjectIssueTypesSchema.optional(),
});

exports.JiraStatusSchema = JiraStatusSchema;
//# sourceMappingURL=jiraStatus.cjs.map
