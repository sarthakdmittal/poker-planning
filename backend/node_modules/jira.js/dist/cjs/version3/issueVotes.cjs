'use strict';

class IssueVotes {
    client;
    constructor(client) {
        this.client = client;
    }
    async getVotes(parameters, callback) {
        const config = {
            url: `/rest/api/3/issue/${parameters.issueIdOrKey}/votes`,
            method: 'GET',
        };
        return this.client.sendRequest(config, callback);
    }
    async addVote(parameters, callback) {
        const config = {
            url: `/rest/api/3/issue/${parameters.issueIdOrKey}/votes`,
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
        };
        return this.client.sendRequest(config, callback);
    }
    async removeVote(parameters, callback) {
        const config = {
            url: `/rest/api/3/issue/${parameters.issueIdOrKey}/votes`,
            method: 'DELETE',
        };
        return this.client.sendRequest(config, callback);
    }
}

exports.IssueVotes = IssueVotes;
//# sourceMappingURL=issueVotes.cjs.map
