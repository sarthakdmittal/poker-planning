'use strict';

var base64Encoder = require('../base64Encoder.cjs');

function createBasicAuthenticationToken(authenticationData) {
    const login = authenticationData.email;
    const secret = authenticationData.apiToken;
    const token = base64Encoder.encode(`${login}:${secret}`);
    return `Basic ${token}`;
}

exports.createBasicAuthenticationToken = createBasicAuthenticationToken;
//# sourceMappingURL=createBasicAuthenticationToken.cjs.map
