'use strict';

var zod = require('zod');

const PageSchema = (valueSchema) => zod.z.strictObject({
    values: valueSchema.array(),
    /** The index of the first item returned. */
    startAt: zod.z.int(),
    /** The maximum number of items that could be returned. */
    maxResults: zod.z.int(),
    /** The number of items returned. */
    total: zod.z.int(),
    /** Whether this is the last page. */
    isLast: zod.z.boolean(),
    self: zod.z.url(),
});

exports.PageSchema = PageSchema;
//# sourceMappingURL=pageSchema.cjs.map
