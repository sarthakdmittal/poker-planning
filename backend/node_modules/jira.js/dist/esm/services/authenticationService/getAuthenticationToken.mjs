import { createBasicAuthenticationToken } from './authentications/createBasicAuthenticationToken.mjs';
import { createOAuth2AuthenticationToken } from './authentications/createOAuth2AuthenticationToken.mjs';

async function getAuthenticationToken(authentication) {
    if (!authentication) {
        return undefined;
    }
    if ('basic' in authentication) {
        return createBasicAuthenticationToken(authentication.basic);
    }
    return createOAuth2AuthenticationToken(authentication.oauth2);
}

export { getAuthenticationToken };
//# sourceMappingURL=getAuthenticationToken.mjs.map
