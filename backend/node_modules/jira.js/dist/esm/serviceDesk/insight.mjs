class Insight {
    client;
    constructor(client) {
        this.client = client;
    }
    async getInsightWorkspaces(parameters, callback) {
        const config = {
            url: '/rest/servicedeskapi/insight/workspace',
            method: 'GET',
            params: {
                start: parameters?.start,
                limit: parameters?.limit,
            },
        };
        return this.client.sendRequest(config, callback);
    }
}

export { Insight };
//# sourceMappingURL=insight.mjs.map
