class Customer {
    client;
    constructor(client) {
        this.client = client;
    }
    async createCustomer(parameters, callback) {
        const config = {
            url: '/rest/servicedeskapi/customer',
            method: 'POST',
            data: {
                email: parameters?.email,
                displayName: parameters?.displayName,
            },
        };
        return this.client.sendRequest(config, callback);
    }
}

export { Customer };
//# sourceMappingURL=customer.mjs.map
