import { z } from 'zod';
import { ProjectIdSchema } from './projectId.mjs';

/** The scope of the status. */
const StatusScopeSchema = z.strictObject({
    /** The scope of the status. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    type: z.enum(['GLOBAL', 'PROJECT']),
    project: ProjectIdSchema.optional(),
});

export { StatusScopeSchema };
//# sourceMappingURL=statusScope.mjs.map
