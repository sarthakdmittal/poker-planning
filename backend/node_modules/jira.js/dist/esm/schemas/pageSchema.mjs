import { z } from 'zod';

const PageSchema = (valueSchema) => z.strictObject({
    values: valueSchema.array(),
    /** The index of the first item returned. */
    startAt: z.int(),
    /** The maximum number of items that could be returned. */
    maxResults: z.int(),
    /** The number of items returned. */
    total: z.int(),
    /** Whether this is the last page. */
    isLast: z.boolean(),
    self: z.url(),
});

export { PageSchema };
//# sourceMappingURL=pageSchema.mjs.map
