import { z } from 'zod';
export declare const PageSchema: <T extends z.ZodSchema | z.ZodType>(valueSchema: T) => z.ZodObject<{
    values: z.ZodArray<T>;
    /** The index of the first item returned. */
    startAt: z.ZodInt;
    /** The maximum number of items that could be returned. */
    maxResults: z.ZodInt;
    /** The number of items returned. */
    total: z.ZodInt;
    /** Whether this is the last page. */
    isLast: z.ZodBoolean;
    self: z.ZodURL;
}, z.core.$strict>;
export type Page<T extends z.ZodType> = z.infer<ReturnType<typeof PageSchema<T>>>;
