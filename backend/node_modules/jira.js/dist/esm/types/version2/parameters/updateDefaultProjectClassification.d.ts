import type { UpdateDefaultProjectClassification as UpdateDefaultProjectClassificationModel } from "../models/index.js";
export interface UpdateDefaultProjectClassification extends UpdateDefaultProjectClassificationModel {
    /** The project ID or project key (case-sensitive). */
    projectIdOrKey: string;
}
