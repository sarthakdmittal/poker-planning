import type * as Models from "./models/index.js";
import type * as Parameters from "./parameters/index.js";
import type { Client } from "../clients/index.js";
import type { Callback } from "../callback.js";
export declare class Api {
    private client;
    constructor(client: Client);
    /**
     * @experimental
     *
     * Returns worklog details for a list of issue ID and worklog ID pairs.
     *
     * This is an internal API for bulk fetching worklogs by their issue and worklog IDs. Worklogs that don't exist will
     * be filtered out from the response.
     *
     * The returned list of worklogs is limited to 1000 items.
     *
     * **[Permissions](https://developer.atlassian.com/cloud/jira/platform/rest/v2/intro/#permissions) required:** This is
     * an internal service-to-service API that requires ASAP authentication. No user permission checks are performed as
     * this bypasses normal user context.
     */
    getWorklogsByIssueIdAndWorklogId<T = Models.BulkWorklogKeyResponse>(parameters: Parameters.GetWorklogsByIssueIdAndWorklogId, callback: Callback<T>): Promise<void>;
    /**
     * @experimental
     *
     * Returns worklog details for a list of issue ID and worklog ID pairs.
     *
     * This is an internal API for bulk fetching worklogs by their issue and worklog IDs. Worklogs that don't exist will
     * be filtered out from the response.
     *
     * The returned list of worklogs is limited to 1000 items.
     *
     * **[Permissions](https://developer.atlassian.com/cloud/jira/platform/rest/v2/intro/#permissions) required:** This is
     * an internal service-to-service API that requires ASAP authentication. No user permission checks are performed as
     * this bypasses normal user context.
     */
    getWorklogsByIssueIdAndWorklogId<T = Models.BulkWorklogKeyResponse>(parameters: Parameters.GetWorklogsByIssueIdAndWorklogId, callback?: never): Promise<T>;
}
