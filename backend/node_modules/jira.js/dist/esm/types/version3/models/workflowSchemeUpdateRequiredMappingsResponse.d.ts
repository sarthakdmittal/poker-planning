import type { RequiredMappingByIssueType } from "./requiredMappingByIssueType.js";
import type { RequiredMappingByWorkflows } from "./requiredMappingByWorkflows.js";
import type { StatusMetadata } from "./statusMetadata.js";
import type { StatusesPerWorkflow } from "./statusesPerWorkflow.js";
export interface WorkflowSchemeUpdateRequiredMappingsResponse {
    /** The list of required status mappings by issue type. */
    statusMappingsByIssueTypes?: RequiredMappingByIssueType[];
    /** The list of required status mappings by workflow. */
    statusMappingsByWorkflows?: RequiredMappingByWorkflows[];
    /** The details of the statuses in the associated workflows. */
    statuses?: StatusMetadata[];
    /** The statuses associated with each workflow. */
    statusesPerWorkflow?: StatusesPerWorkflow[];
}
